# Repository count

**Total GitHub repos needed: 1**

This project uses a **single monorepo**. One repo contains:

| Folder / area   | Purpose                |
|----------------|------------------------|
| `client/`      | React + Vite frontend  |
| `server/`      | Node.js + Express API  |
| `ai-service/`  | Python Flask AI service |

All deployments (Vercel + Render backend + Render AI) connect to this **same repository**; they use different build commands and roots.

See **2.md** for how each deployment uses this repo.
